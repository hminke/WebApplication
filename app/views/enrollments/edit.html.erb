<h1>Edit Enrollment</h1>

<%=  form_for :enrollment, url:enrollment_path, method: :patch, local: true do |form| %>

  <% if @enrollment.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@section.errors.count, "error") %> prohibited
        this section from being saved:
      </h2>
      <ul>
        <% @enrollment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :student_id, 'Student' %><br>
    <%= form.collection_select(:student_id, Student.all, :id, :name) %>
  </p>

  <p>
    <%= form.label :section_id, 'Course' %><br>
    <%= form.collection_select(:section_id, Section.all, :id, :course) %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>

<p>
  <%= link_to 'View Enrollment', sections_path %>
<p/>

<p>
  <a href="/" >Home</a>
</p>